var Waterline = require('waterline')

/**
 * Defines a Rainbow collection which in turn
 * will generate a set of routes automatically,
 * all the backbone routes and backbone collections/models
 * for use in the front end without having to do any
 * plumbing work and crack on with the theming and business
 * logic in your app and API.
 *
 * @return {Object} Rainbow.Collection
 */
module.exports = function RainbowCollection() {
  // Get the collection from Waterline.
  var Collection = require('waterline').Collection

}
